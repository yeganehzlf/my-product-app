//Defining a functional component named "header"

const Header = () =>{
    //this component returns a JSX element, a styled heading.
    return <h1>Welcom to product management.</h1>;
};
//Exporting the Header component so it can be used in other parts the application
export default Header;